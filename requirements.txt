# App-only dependencies. Streamlit Community Cloud installs this file.
# For local development (data pipeline + notebooks) use requirements-dev.txt.
#
# The --extra-index-url pulls the CPU-only PyTorch build (~200 MB) instead of
# the default CUDA-bundled wheel (~2.5 GB), which is too large for the free tier.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit>=1.62.0
torch>=2.1.0
torchvision>=0.16.0
opencv-python-headless>=4.8.0   # -headless: no libGL needed on a headless server
pillow>=10.0.0
numpy>=1.24.0
