# Full local development environment: everything the app needs, plus the data
# pipeline (src/download_raw_images.py ... src/generate_combos.py, src/metrics.py)
# and the training notebooks.
#
#   pip install -r requirements-dev.txt
#
# (GPU users: install a CUDA PyTorch build first via conda, then run this -- pip
#  will see torch already satisfied and skip it. See SETUP.md.)

-r requirements.txt

pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
requests>=2.31.0
jupyter>=1.0.0
ipykernel>=6.25.0
nbformat>=5.9.0
